# API Configuration

# IMPORTANT: Update this with your computer's IP address
# Find your IP: Run "ipconfig" in PowerShell (Windows) or "ifconfig" in Terminal (Mac/Linux)
# Example: 192.168.1.100

# For local testing on Expo Go:
# Use your computer's local IP address, NOT localhost or 127.0.0.1

API_BASE_URL=http://localhost:8000/api

# Steps to configure:
# 1. Open PowerShell/Terminal
# 2. Run: ipconfig (Windows) or ifconfig (Mac/Linux)
# 3. Find your IPv4 Address (e.g., 192.168.1.100)
# 4. Replace 'localhost' above with your IP
# 5. Example: http://192.168.1.100:8000/api

# Make sure:
# - Backend server is running on port 8000
# - Your phone and computer are on the same WiFi network
# - Firewall allows connections on port 8000
